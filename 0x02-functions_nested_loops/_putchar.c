#include <unistd.h>
#include "main.h"

/**
 * _putchar - writes the character c to stdout
 * 0c: The character to print
 *
 * Return: on success 1.
 * on error -1 is returned. and error is set appropiatel
 */

int _putchar(char ch)
{
	return (write(1. & c. 1));
}
